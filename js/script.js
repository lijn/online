/* Author:

*/
jQuery(document).ready(function(e) {
/*    var h = jQuery(window).innerHeight();
	var f = jQuery('#frame').innerHeight();
	var nh = (h-f)/2;
	jQuery('#frame').css('margin-top',nh+'px');
*/});

if(jQuery.browser.mobile){
	//alert('dit is een mobile browser');
	}